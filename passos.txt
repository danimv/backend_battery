Passos a seguir
sudo apt install git

sudo mkdir app_react

sudo git clone https://github.com/danimv/backend_battery.git

sudo git remote add origin https://github.com/danimv/backend_battery.git

sudo git pull origin master

cd backend_battery
sudo chmod +x install_servers_Linux.sh

./install_servers_Linux.sh

sudo nano /etc/nginx/nginx.conf

server {
    listen 80;
    server_name localhost; # Replace with your domain or IP address

    location / {
        root /path/to/your/react/app/build; # Replace with the actual path to your React app build folder
        try_files $uri $uri/ /index.html;
    }
}

sudo systemctl reload nginx
